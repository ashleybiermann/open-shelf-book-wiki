<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <link rel="stylesheet" href="/styles/reset.css">
  <link rel="stylesheet" href="/styles/base.css">
  <link rel="stylesheet" href="/styles/layout.css">
  <link rel="stylesheet" href="/styles/modules.css">
  <title>Search Results</title>
</head>
<body>
  <%- include('../../../views/partials/nav.ejs') %>

  <h1>Your Results</h1>

  <ul>
    <% books.forEach(current => { %>
      <li> 
        <img src="<%=current.image %> alt="book cover image">
        <h2><%=current.title %></h2>
        <h3><%=current.author %></h3>
        <p><%=current.description %></p>
        <h6>isbn: <%=current.isbn %></h6>
        <h4>bookshelf: <%=current.bookshelf %></h4>
      </li>

      <form action="/books" method="POST">
        <fieldset> 
          <input type="hidden" name="author" value="<%=current.author %>">
          <input type="hidden" name="title" value="<%=current.title %>">
          <input type="hidden" name="isbn" value="<%=current.isbn %>">
          <input type="hidden" name="image_url" value="<%=current.image %>">
          <input type="hidden" name="description" value="<%=current.description %>">
          <input type="hidden" name="bookshelf" value="<%=current.bookshelf %>">

          <button type="submit">Save to My Collection</button>
        </fieldset>
      </form>
      <% }) %>
    </ul>
    
    <footer></footer>

  </body>
  </html>
  
  <!-- build form for user to save a book -->
  <!-- form will reference all of the values that are coming from current. Right above this :) Will have input fields for all of this. Value fields = exactly what is right up there. Tie them together with a single submit button that will send it to the backend (server). Server takes the form inputs and turns it into the INSERT statement for sql. app.post('/books'); path. this will be the action="/books " on form. method="post" on server. Call path /books-->
  <!--  app.post('/books')  ... console.log(req.body); -->
